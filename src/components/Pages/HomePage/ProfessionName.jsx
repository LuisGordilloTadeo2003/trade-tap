import React from "react";

const Profession = (props) => {
    return (
        <div>
            <p style={{ fontSize: "12px", marginTop: "3px" }}>{props.profesion}</p>
        </div>
    );
}

export default Profession;